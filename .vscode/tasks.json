{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Python Competitive",
            "type": "shell",
            "command": "python3 -u ${file} < ${fileDirname}/${fileBasenameNoExtension}.in > ${fileDirname}/${fileBasenameNoExtension}.out 2>&1",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "C# Dot Net",
            "type": "shell",
            "command": "dotnet run",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}